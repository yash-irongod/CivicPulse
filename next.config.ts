import type { NextConfig } from "next";

// Empty at scaffold (Task 1.1) — populated as later tasks need it
// (e.g. image domains, headers) rather than pre-guessed here.
const nextConfig: NextConfig = {};

export default nextConfig;
