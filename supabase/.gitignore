# Supabase CLI local runtime state — machine-specific, regenerated by
# `supabase link` / `supabase start` / `supabase branches`. Never commit it:
# it can hold the linked project's ref and other local-only CLI state.
.branches
.temp
